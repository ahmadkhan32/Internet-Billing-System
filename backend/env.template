# Internet Billing System - Environment Variables Template
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to .env (remove .template)
# 2. Fill in your actual values (replace the placeholders)
# 3. NEVER commit .env to Git (it's already in .gitignore)
#
# For Vercel Deployment:
# - Copy these values to Vercel → Settings → Environment Variables
# - Set for Production environment
# - Redeploy after adding variables

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================

# Database Host
# Examples:
#   - Local: localhost
#   - PlanetScale: aws.connect.psdb.cloud
#   - AWS RDS: your-db.xxxxx.us-east-1.rds.amazonaws.com
#   - Railway: containers-us-west-xxx.railway.app
DB_HOST=localhost

# Database Username
# Examples: root, admin, username
DB_USER=root

# Database Password
# IMPORTANT: 
#   - For local: Can be empty if MySQL has no password (DB_PASSWORD=)
#   - For Vercel/Production: MUST be non-empty!
#   - Get this from your database provider dashboard
DB_PASSWORD=your_database_password_here

# Database Name
# Examples: billing_db, internet_billing_db, defaultdb
DB_NAME=internet_billing_db

# ============================================
# JWT CONFIGURATION (REQUIRED)
# ============================================

# JWT Secret Key
# MUST be at least 32 characters long
# Generate one: openssl rand -base64 32
# Or use: https://randomkeygen.com/
JWT_SECRET=your_random_secret_key_minimum_32_characters_long_12345678901234567890

# JWT Expiration (optional)
JWT_EXPIRE=7d

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server Port (for local development)
PORT=8000

# Node Environment
# - development: For local development
# - production: For Vercel/production
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3001

